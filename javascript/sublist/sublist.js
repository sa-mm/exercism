const List = function (list = []) {
  this.list = list
  this.length = this.list.length
}

const listCheck = ([head, ...tail], arr2, start) => {
  // const start = arr2.indexOf(head)
  // if (start >= 0) {

  // }
  var inc = 1
  while (i <= arr1.length) {
    let item = arr1[i]
    let start = arr2.indexOf(item)
    if (start >= 0) {

      
    }
  }
  for (let i = 0, i <= arr1.length; i += inc) {
    let item = arr1[i]
    let start = arr2.indexOf(item)
    if (start >= 0) {

    }
  }
}

List.prototype.compare = function (anotherList) {
  const length = this.length
  const list = this.list

}

module.exports = List

// var test = new List()
// console.log(test.list)
